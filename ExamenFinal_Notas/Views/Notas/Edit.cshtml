@{ Layout = null;
    var nota = (Nota)Model;}

<div class="bg-info">
    <div class="col-6">
        <form id="Edit" onsubmit="editar(event)" action="#" enctype="multipart/form-data" method="post">
            <input id="mtxtId" type="hidden" name="Id" />

            <div class="form-group">
                <h1 style="font-size:20px">Titulo</h1>
                <input id="mtxtTitulo" type="text" class="input-group-text" name="Tittle" placeholder="Titulo"/>
                @Html.ValidationMessage("Tittle")
            </div>

            <div class="form-group">
                <h1 style="font-size:20px">Contenido</h1>
                <textarea class="text-body" id="mtxtCuerpo" type="text" name="Nota_Content" rows="4" cols="50"></textarea>
                @Html.ValidationMessage("Nota_Content")
            </div>



            @*<div class="form-group">
                <label>Etiquetas</label>
                @foreach (var item in ViewBag.Etiquetas)
                {
                    <div class="form-check form-check">
                        <input name="etiqueta" class="form-check-input" type="checkbox" id="inlineCheckbox1" value="@item.Id">
                        <label class="form-check-label"
                               for="inlineCheckbox1">@item.Name</label>
                        </div>
                    }
                @Html.ValidationMessage("etiqueta")
            </div>*@
        </form>
    </div>
</div>
